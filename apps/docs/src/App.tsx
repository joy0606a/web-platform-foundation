import { Button } from "@repo/ui/button";

export function App() {
  return (
    <main>
      <h1>Docs</h1>
      <p>Vite + React SPA + @repo/ui</p>
      <Button appName="docs">Click me</Button>
    </main>
  );
}
